## Packages
framer-motion | For smooth page transitions and micro-interactions
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes
lucide-react | Icon set (already in base but good to confirm)

## Notes
Tailwind config needs to be updated with custom colors: ttblack, ttbg, ttpink, ttcyan, ttgray.
The app uses a dark theme by default.
Viral score calculation logic needs to be implemented in client/src/lib/viralScore.ts.
API endpoints are defined in shared/routes.ts and should be accessed via hooks.
